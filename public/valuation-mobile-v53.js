(()=>{'use strict';
const mobile=()=>window.matchMedia('(max-width:700px)').matches;
function byHeading(text){for(const h of document.querySelectorAll('.card h3')){if((h.textContent||'').includes(text))return h.closest('.card')}return null}
function markAnchors(){const map={plan:document.getElementById('v50Plan'),clients:byHeading('Клиенты и сделки'),follow:document.getElementById('v52Follow'),top:document.querySelector('.hero')};for(const el of Object.values(map))if(el)el.classList.add('v53-anchor');return map}
function scrollToEl(el){if(!el)return;el.scrollIntoView({behavior:'smooth',block:'start'})}
function mount(){if(!mobile()||document.querySelector('.v53-dock'))return;document.body.classList.add('v53-mobile');const title=document.querySelector('.top h1');if(title){title.dataset.fullTitle=title.textContent||'';title.textContent='📥 CRM · V53'}const map=markAnchors();const dock=document.createElement('nav');dock.className='v53-dock';dock.setAttribute('aria-label','Быстрая навигация CRM');dock.innerHTML=`<button type="button" data-go="plan"><span>📅</span>План</button><button type="button" data-go="clients"><span>👥</span>Клиенты</button><button type="button" data-go="follow"><span>🔁</span>Follow-up</button><button type="button" data-go="top"><span>⬆️</span>Вверх</button>`;document.body.appendChild(dock);dock.querySelectorAll('button').forEach(b=>b.onclick=()=>{const key=b.dataset.go;if(key==='top')window.scrollTo({top:0,behavior:'smooth'});else scrollToEl(map[key]);dock.querySelectorAll('button').forEach(x=>x.classList.toggle('on',x===b))});
const sections=[['plan',map.plan],['clients',map.clients],['follow',map.follow]].filter(x=>x[1]);if('IntersectionObserver'in window){const obs=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(!visible)return;const hit=sections.find(x=>x[1]===visible.target);if(!hit)return;dock.querySelectorAll('button').forEach(x=>x.classList.toggle('on',x.dataset.go===hit[0]))},{rootMargin:'-25% 0px -55% 0px',threshold:[0,.1,.25,.5]});sections.forEach(x=>obs.observe(x[1]))}
}
function sync(){if(mobile()){mount()}else{const d=document.querySelector('.v53-dock');if(d)d.remove();const title=document.querySelector('.top h1');if(title?.dataset.fullTitle)title.textContent=title.dataset.fullTitle}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(sync,80));else setTimeout(sync,80);window.addEventListener('resize',()=>setTimeout(sync,80));
})();
